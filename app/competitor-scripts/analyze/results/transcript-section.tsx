"use client";

import { useState } from "react";
import { ChevronDown } from "lucide-react";
import type { Messages } from "../../../../lib/messages";
import type { TranscriptSegment } from "../../../../lib/competitor-scripts/transcript/types";
import { formatTimestampMs } from "./format-timestamp";

type TranscriptCopy =
  Messages["competitorScripts"]["analyzeResults"]["transcript"];

const SEGMENT_PREVIEW_COUNT = 6;

export type TranscriptSectionData = {
  languageCode: string | null;
  isAutoGenerated: boolean | null;
  durationMs: number | null;
  segments: TranscriptSegment[];
  text: string;
};

// The one place on the Results page that renders real, API-derived
// transcript content. Text is always rendered as plain React text, never
// through React's raw-HTML injection prop, so nothing in a transcript can
// execute as markup — this is untrusted video-derived content. Only the
// timestamped segment list is progressively disclosed (a long transcript
// can have thousands of segments); the full transcript text itself is
// always shown in full, never collapsed.
export function TranscriptSection({
  copy,
  data,
}: {
  copy: TranscriptCopy;
  data: TranscriptSectionData;
}) {
  const [showAllSegments, setShowAllSegments] = useState(false);

  // Generation (auto-generated vs. manual) is only shown when the
  // provider actually told us — there is no reliable source metadata to
  // fall back on, so an unknown value is omitted entirely rather than
  // shown as a meaningless "Source: Unknown" stat.
  const stats = [
    { label: copy.languageLabel, value: data.languageCode ?? copy.unknownLanguage },
    {
      label: copy.durationLabel,
      value:
        data.durationMs === null
          ? copy.unknownDuration
          : formatTimestampMs(data.durationMs),
    },
    { label: copy.segmentCountLabel, value: String(data.segments.length) },
    ...(data.isAutoGenerated !== null
      ? [
          {
            label: copy.generationLabel,
            value: data.isAutoGenerated
              ? copy.autoGeneratedLabel
              : copy.manualLabel,
          },
        ]
      : []),
  ];

  const hasMoreSegments = data.segments.length > SEGMENT_PREVIEW_COUNT;
  const visibleSegments = showAllSegments
    ? data.segments
    : data.segments.slice(0, SEGMENT_PREVIEW_COUNT);

  return (
    <section className="rounded-[20px] border border-[#7C3AED]/15 bg-white/[0.035] p-5 lg:p-7">
      <div className="flex flex-wrap items-center justify-between gap-3">
        <p className="text-[12px] font-semibold uppercase tracking-[0.14em] text-[#A78BFA]">
          {copy.sectionEyebrow}
        </p>
        <span className="inline-flex items-center rounded-full border border-[#7C3AED]/30 bg-[#7C3AED]/10 px-3 py-1 text-[11px] font-semibold uppercase tracking-[0.08em] text-[#C4B5FD]">
          {copy.realDataLabel}
        </span>
      </div>
      <h2 className="mt-2 text-[19px] font-semibold text-[#F5F5F7]">
        {copy.heading}
      </h2>

      <dl
        className={`mt-4 grid grid-cols-2 gap-x-4 gap-y-3 ${
          stats.length >= 4 ? "sm:grid-cols-4" : "sm:grid-cols-3"
        }`}
      >

        {stats.map((stat) => (
          <div key={stat.label}>
            <dt className="text-[10.5px] font-medium uppercase tracking-[0.06em] text-[#6B7280]">
              {stat.label}
            </dt>
            <dd className="mt-0.5 truncate text-[13px] font-medium text-[#D1D5DB]">
              {stat.value}
            </dd>
          </div>
        ))}
      </dl>

      <div className="mt-6">
        <p className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#6B7280]">
          {copy.fullTextHeading}
        </p>
        <p className="mt-3 max-w-[760px] whitespace-pre-wrap break-words text-[13.5px] leading-[1.7] text-[#D1D5DB]">
          {data.text}
        </p>
      </div>

      {data.segments.length > 0 && (
        <div className="mt-6">
          <p className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#6B7280]">
            {copy.timestampedHeading}
          </p>
          <ul className="mt-3 flex flex-col gap-3">
            {visibleSegments.map((segment, index) => (
              <li
                key={`${segment.startMs}-${index}`}
                className="flex items-start gap-3"
              >
                <span className="w-[52px] shrink-0 text-[12px] font-semibold tabular-nums text-[#A78BFA]">
                  {formatTimestampMs(segment.startMs)}
                </span>
                <p className="min-w-0 flex-1 break-words text-[13.5px] leading-[1.6] text-[#D1D5DB]">
                  {segment.text}
                </p>
              </li>
            ))}
          </ul>

          {hasMoreSegments && (
            <button
              type="button"
              onClick={() => setShowAllSegments((current) => !current)}
              aria-expanded={showAllSegments}
              className="mt-3 inline-flex items-center gap-1 bg-transparent text-[12.5px] font-semibold text-[#A78BFA] transition-colors hover:text-[#C4B5FD] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#C4B5FD]"
            >
              {showAllSegments ? copy.showFewerSegments : copy.showAllSegments}
              <ChevronDown
                size={13}
                className={`transition-transform ${showAllSegments ? "rotate-180" : ""}`}
                aria-hidden="true"
              />
            </button>
          )}
        </div>
      )}
    </section>
  );
}
